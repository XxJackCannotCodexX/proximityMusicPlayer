async function connectToAirPods() {
  try {
    return await navigator.bluetooth.requestDevice({
      filters: [{ services: ['battery_service'] }],
      acceptAllDevices: false, 
      optionalServices: ['device_information'] 
    });
    await connectToGATTServer(device);
  } catch (error) {
    return "error"
  }
  console.log("clicked")
}

const p = document.getElementById("rssi")
const button = document.getElementById("click")
const device = button.addEventListener("click", connectToAirPods)
// while(true){ 
//     p.innerHTML = device
// }
while(true){ 
    p.innerHTML = device.rssi
}
    

